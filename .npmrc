# Always generate and respect package-lock.json
package-lock=true

# Install exactly the versions you specify in package.json
save-exact=true

# If you hit peer-dependency conflicts, you can fall back to:
# legacy-peer-deps=true

# Turn off audit warnings in CI or noisy environments
# audit=false

# Use a custom registry (if you have one):
# registry=https://registry.npmjs.org/

# Ensure you aren’t accidentally pulling from HTTP mirrors
strict-ssl=true

# If you care about engines (node/npm versions), you can enforce them:
# engine-strict=true
